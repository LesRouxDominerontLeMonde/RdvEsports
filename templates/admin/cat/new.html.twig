{%extends 'admin/admin.html.twig'%}

{%block title%}New Category{%endblock%}

{%block body%}
    <h1>New Category</h1>

    {{form_start(form)}}
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
    {{form_end(form)}}
{%endblock%}